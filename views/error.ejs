<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { padding-top: 3rem; }
    </style>
</head>
<body class="bg-light">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 text-center">
                <h1 class="display-4 text-danger">Oops! Something went wrong.</h1>
                <p class="lead"><%= message %></p>

                <%# Only show stack trace in development %>
                <% if (process.env.NODE_ENV !== 'production' && error && error.stack) { %>
                    <div class="card mt-4">
                        <div class="card-header">Error Details (Development Mode)</div>
                        <div class="card-body text-start">
                            <pre><code><%= error.stack %></code></pre>
                        </div>
                    </div>
                <% } %>

                <hr class="my-4">
                <p>You can try returning to the previous page or go to the main inventory page.</p>
                <a href="/inventory" class="btn btn-primary">Go to Inventory</a>
                <a href="javascript:history.back()" class="btn btn-secondary">Go Back</a>
            </div>
        </div>
    </div>
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>